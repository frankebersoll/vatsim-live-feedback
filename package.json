{
    "private": true,
    "scripts": {
        "postinstall": "dotnet tool restore",
        "start": "dotnet fable watch --cwd src/Client -o output --run parcel index.html",
        "bundle": "dotnet fable --cwd src/Client -o output --run parcel build index.html --dist-dir ../../deploy/public"
    },
    "devDependencies": {
        "http-proxy-middleware": "^2.0.1",
        "parcel": "^2.0.1",
        "react": "^16.10.2",
        "react-dom": "^16.10.2"
    },
    "packageManager": "yarn@3.1.0"
}
